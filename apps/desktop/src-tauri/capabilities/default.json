{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set for the LangTextFlow operator window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
